# best/ and papers/ ARE committed (unified into this single repo; nested .git dirs stripped).
# Reproduce/refresh them with scripts/fetch_pool.sh if needed.

# Caches only
**/__pycache__/
*.pyc
lab/**/ada.json
